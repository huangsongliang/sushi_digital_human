apiVersion: v1
kind: Service
metadata:
  name: {{ include "sushi-digital-human.fullname" . }}-api
  labels:
    {{- include "sushi-digital-human.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.api.type }}
  ports:
    - port: {{ .Values.service.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sushi-digital-human.apiSelectorLabels" . | nindent 4 }}
